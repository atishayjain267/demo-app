# Demo App

A lightweight Python application demonstrating environment isolation via `venv` and HTTP API requests using the `requests` library.

## Setup

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python main.py
```
